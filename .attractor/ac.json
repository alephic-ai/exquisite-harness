{
  "acs": [
    {
      "id": 1,
      "criterion": "`eh run --read-only` maps each of the five harnesses to its verified mechanism exactly per the spike's table, via a new mapping module analogous to `src/approval-mode.ts` (not an extension of `APPROVAL_MODES` — approval and write-restriction are independent axes). Proof: per-harness subprocess tests asserting the exact args, per the `createFakeHarness` pattern.",
      "status": "pass",
      "evidence": "New module src/permission-posture.ts (permissionArgsForHarness) mirrors approval-mode.ts; APPROVAL_MODES unchanged. Per-harness subprocess tests in src/headless-run.test.ts ('engages %s read-only args under platform approval') assert exact args: claude/grok --permission-mode plan, codex --sandbox read-only, opencode --agent plan, pi --tools read,grep,find,ls. `bun test src/headless-run.test.ts -t read-only` -> 8 pass, 0 fail."
    },
    {
      "id": 2,
      "criterion": "Invariant: a lane started with `--read-only` is never silently unrestricted — a harness the spike marked \"no mechanism\" behaves exactly as the spike decided (refuse to launch or loud warning, per the decision), with a test for that path.",
      "status": "pass",
      "evidence": "readOnlyArgs() ends in unreachable(harness: never) that throws 'no read-only mechanism ... refusing to launch a --read-only lane'. src/permission-posture.test.ts asserts the throw ('refuses to launch a read-only lane for a harness with no mechanism') and non-empty args for all 5 real harnesses ('never leaves %s silently unrestricted under read-only'). 15 pass, 0 fail."
    },
    {
      "id": 3,
      "criterion": "`--read-only` composes with `defaultApprovalMode: auto` per the spike's decided interaction (both active in one run), with a test covering the combined args.",
      "status": "pass",
      "evidence": "Composition subprocess tests in src/headless-run.test.ts write {defaultApprovalMode:'auto'} config then run --read-only: opencode -> --agent plan --auto (compose); codex -> --sandbox read-only, NOT --approve-for-me (suppress); claude -> --permission-mode plan, NOT auto (suppress). Unit tests in src/permission-posture.test.ts confirm the same resolution. All pass."
    }
  ]
}
